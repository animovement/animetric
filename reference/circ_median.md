# Calculate circular median

Computes the median of circular data using Fisher's definition.

## Usage

``` r
circ_median(x, na_rm = TRUE)
```

## Arguments

- x:

  Numeric vector of angles in radians

- na_rm:

  Logical; if TRUE, remove NA values before computation

## Value

The circular median as a single numeric value in radians
