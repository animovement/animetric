# The variable roles an aniframe declares

The variable roles an aniframe declares

## Usage

``` r
nnd_variables(data)
```

## Arguments

- data:

  An aniframe.

## Value

Named list of `what`, `when` and `where`, each restricted to columns
actually present in the data.
