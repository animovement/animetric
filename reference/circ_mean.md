# Circular mean

Circular mean

## Usage

``` r
circ_mean(x, na_rm = TRUE)
```

## Arguments

- x:

  Numeric vector of angles in radians

- na_rm:

  Logical; if TRUE, remove NA values before computation

## Value

Mean direction (radians) wrapped to \[0, 2\*pi)
